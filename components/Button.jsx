import React from "react";

const Button = ({ styles }) => {
  return (
    <button
      type="button"
      className="mt-8 py-4 px-6 bg-blue-gradient font-poppins text-primary outline-none rounded-[10px]"
    >
      Get Started
    </button>
  );
};

export default Button;
